# Keep this list in place so ASGI startup does not fail when websocket
# routes are not yet implemented.
websocket_urlpatterns = [
    # Example:
    # re_path(r"ws/example/$", ExampleConsumer.as_asgi()),
]
